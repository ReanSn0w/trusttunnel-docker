# Vendored frontend assets

- Bootstrap 5.3.8 (`bootstrap.min.css`, `bootstrap.bundle.min.js`), MIT.
- htmx 2.0.6 (`htmx.min.js`), BSD-2-Clause.

`ASSETS.sha256` records the exact downloaded bytes. The application serves
these files from the embedded filesystem and never references a CDN.
