---
Название: "Ядро Go-контроллера TrustTunnel"
Сложность: hard
Важность: 1
Дата завершения: 2026-09-23
---

## Что мы делаем?

Создаём фундамент self-contained образа: Go-контроллер работает PID 1, хранит состояние в SQLite, безопасно генерирует TOML-конфиги и управляет официальным `trusttunnel_endpoint` как дочерним процессом. В границы плана входят CLI, генерация клиентских конфигов через официальный бинарник, health/readiness, чтение Prometheus-метрик, базовый multi-arch образ и тесты. ACME и Web UI вынесены в отдельные планы.

## Для чего мы делаем?

Текущий проект запускает официальный endpoint через shell-entrypoint и хранит сгенерированные файлы в одном Docker volume. Для безопасного Web-управления нужен единый слой состояния, валидации, транзакционного применения и корректного жизненного цикла дочернего процесса без переписывания самого протокола.

## План

- [x] Зафиксировать границы контроллера и контракты с официальным endpoint
  - [x] Описать единый жизненный цикл: загрузка состояния, валидация, материализация TOML, запуск endpoint, мониторинг и остановка.
  - [x] Разделить операции, требующие полного restart, и изменения TLS hosts, достаточные для SIGHUP.
  - [x] Задать интерфейсы repository, renderer, validator, process supervisor, endpoint CLI, metrics reader и event log без зависимости от HTTP-слоя.

- [x] Создать Go-каркас приложения
  - [x] Инициализировать Go module и точку входа контроллера.
  - [x] Разнести domain-модели, persistence, генерацию конфигов, supervisor, CLI-adapter и HTTP probes по изолированным внутренним пакетам.
  - [x] Подключить `github.com/go-pkgz/lgr` к структурным логам без вывода credentials, session tokens и закрытых ключей.
  - [x] Подключить `github.com/jessevdk/go-flags` для параметров путей, listener'ов, таймаутов, режима логов и бинарника endpoint.
  - [x] Задать безопасные значения по умолчанию для каталогов данных, UI listener и внутреннего metrics listener.

- [x] Спроектировать SQLite schema и миграции
  - [x] Ввести версионируемую schema для администратора, VPN-пользователей, их статуса, настроек endpoint и истории применений.
  - [x] Отделить хеши админских паролей от VPN credentials, которые endpoint требует в обратимом виде.
  - [x] Добавить ограничения уникальности, допустимых статусов, внешние ключи и индексы для частых операций.
  - [x] Реализовать транзакционные repository-операции и контекстную отмену.
  - [x] Настроить WAL, busy timeout, foreign keys и один контролируемый writer для предсказуемого применения.
  - [x] Обеспечить идемпотентный bootstrap и пошаговые миграции без автоматического отката схемы назад.

- [x] Реализовать типизированную модель конфигурации
  - [x] Описать поддерживаемое MVP-подмножество `vpn.toml`, `hosts.toml`, `credentials.toml` и `rules.toml` без потери безопасных значений по умолчанию.
  - [x] Валидировать listen address, порты, hostname/SNI, пути к файлам, имена пользователей, лимиты и правила до записи на диск.
  - [x] Исключить TOML injection за счёт корректного encoder'а или строгого escaping всех значений.
  - [x] Сделать рендер детерминированным, чтобы неизменное состояние не вызывало ложные restart.

- [x] Добавить атомарную материализацию и rollback конфигов
  - [x] Записывать новый набор TOML в закрытый staging-каталог на том же filesystem.
  - [x] Применять безопасные permissions к каталогам, конфигам и credentials до их публикации.
  - [x] Выполнять fsync файлов и каталога перед rename для устойчивости к аварийному завершению.
  - [x] Валидировать staged-набор до замены активной версии.
  - [x] Хранить одну последнюю рабочую ревизию для отката.
  - [x] Откатывать файлы и SQLite-статус при ошибке валидации, таймауте старта или мгновенном падении endpoint.
  - [x] Сериализовать операции apply блокировкой, чтобы параллельные мутации не перемешивали ревизии.

- [x] Реализовать supervisor официального `trusttunnel_endpoint`
  - [x] Запускать endpoint с явными argv, working directory, stdout/stderr pipes и без shell.
  - [x] Ограничить объём буферизируемых логов и удалять секреты перед публикацией событий.
  - [x] Отслеживать PID, exit code, время старта, причину остановки и текущую ревизию конфига.
  - [x] Реализовать controlled restart с graceful terminate, timeout и kill как последним шагом.
  - [x] Передавать SIGHUP только запущенному endpoint и фиксировать результат.
  - [x] Перехватывать SIGTERM/SIGINT в PID 1, прекращать приём запросов, останавливать endpoint и дожидаться всех дочерних процессов.
  - [x] Перезапускать endpoint после неожиданного exit с ограниченным backoff и circuit breaker от crash loop.
  - [x] Вызывать wait для каждого дочернего процесса и покрыть zombie-сценарии тестами.

- [x] Обернуть официальный CLI для клиентских конфигов
  - [x] Вызывать `trusttunnel_endpoint` в режиме экспорта для заданного активного пользователя и публичного адреса.
  - [x] Получать `tt://` deeplink без самостоятельной реализации протокольного формата.
  - [x] Получать client TOML через тот же официальный CLI-adapter.
  - [x] Ограничить timeout, размер stdout/stderr и число параллельных экспортов.
  - [x] Отклонять экспорт для disabled или revoked пользователей.

- [x] Добавить health, readiness и чтение метрик endpoint
  - [x] Отделить liveness самого Go-процесса от readiness SQLite, файлов конфига и endpoint listener'ов.
  - [x] Возвращать машиночитаемый статус без секретов и полных логов.
  - [x] Включать Prometheus listener endpoint на непубличенном внутреннем адресе.
  - [x] Читать и парсить только нужные для MVP серии с ограничением timeout и response size.
  - [x] Отличать недоступные метрики от неготового endpoint и не ронять из-за этого контроллер.

- [x] Собрать базовый single-container образ
  - [x] Создать multi-stage Docker build для статического Go-бинарника на `linux/amd64` и `linux/arm64`.
  - [x] Скачивать официальный endpoint фиксированной версии отдельно для каждой архитектуры.
  - [x] Проверять архитектурный SHA-256 до распаковки release-архива.
  - [x] Зафиксировать minimal runtime-базу и необходимые CA certificates без package manager в итоговом слое.
  - [x] Запускать Go-контроллер как PID 1 без shell-entrypoint.
  - [x] Запускать runtime от непривилегированного UID/GID с правами только на persistent data directory и нужные listener'ы.
  - [x] Не добавлять Docker socket, privileged mode, root SSH, `NET_ADMIN` и `NET_RAW`.

- [x] Покрыть ядро автоматическими проверками
  - [x] Добавить unit-тесты repository, миграций, валидации, TOML-rendering и классификации restart/SIGHUP.
  - [x] Добавить fault-injection тесты атомарной записи и rollback при невалидном конфиге, crash и timeout.
  - [x] Добавить integration-тесты supervisor с fake child process для start, restart, SIGHUP, SIGTERM, crash loop и zombie reaping.
  - [x] Добавить integration-тесты экспорта deeplink/TOML с зафиксированным официальным endpoint.
  - [x] Прогнать `go test`, race detector, `go vet` и статический анализ на поддерживаемых архитектурах.
  - [x] Собрать оба архитектурных образа и проверить версии Go-контроллера и endpoint внутри каждого образа.
